{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"s\u016btram (\u0c38\u0c42\u0c24\u0c4d\u0c30\u0c02)","text":"<p>The thread that connects your code to any language model.</p> <p>Sutram is a lightweight Python library that provides a unified interface for calling LLMs \u2014 with built-in caching, retry policies, and multi-turn session management.</p>"},{"location":"#why-sutram","title":"Why Sutram?","text":"<ul> <li>One interface, many providers \u2014 Switch between OpenRouter, OpenAI, Anthropic, and more without changing your code</li> <li>Built-in caching \u2014 Never pay for the same API call twice</li> <li>Automatic retries \u2014 Handle transient failures with configurable backoff strategies</li> <li>Multi-turn sessions \u2014 Manage conversation history effortlessly</li> <li>Streaming \u2014 Real-time token-by-token output with sync and async generators</li> <li>Sync &amp; Async \u2014 Full support for both workflows</li> <li>Tool Calling \u2014 Define tools with <code>@tool</code> decorator and let LLMs call your functions</li> <li>Structured Output \u2014 Get validated Pydantic model responses with automatic retry</li> <li>Extensible \u2014 Add custom providers in minutes</li> </ul> <p>LLM-Friendly Docs</p> <p>This project provides an llms.txt file following the llmstxt.org standard, making it easy for LLMs and AI agents to understand and use Sutram.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install sutram\n</code></pre>"},{"location":"#minimal-example","title":"Minimal Example","text":"<pre><code>from sutram import create_provider\n\nprovider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=\"your-api-key\",\n)\n\nresponse = provider.call_llm(\"What is the meaning of s\u016btram?\")\nprint(response.content)\n</code></pre>"},{"location":"#next-steps","title":"Next Steps","text":"<ul> <li>Quick Start \u2014 Get up and running in 5 minutes</li> <li>Providers \u2014 Configure and create custom providers</li> <li>Caching \u2014 Speed up development with response caching</li> <li>Sessions \u2014 Build multi-turn chat applications</li> <li>Retry Policies \u2014 Handle API failures gracefully</li> <li>API Reference \u2014 Full API documentation ```</li> </ul>"},{"location":"api-reference/","title":"API Reference","text":""},{"location":"api-reference/#provider-factory","title":"Provider Factory","text":""},{"location":"api-reference/#create_provider","title":"<code>create_provider</code>","text":"<pre><code>create_provider(\n    name: str,\n    model: str,\n    api_key: str | Callable[[], str],\n    base_url: str | None = None,\n    max_retries: int = 0,\n    backoff_factor: float = 1.0,\n    strategy: Literal[\"exponential\", \"fixed\"] = \"exponential\",\n    timeout: int = 120,\n    retry_on_status: list[int] | None = None,\n    cache: Cache | None = None,\n    sync_client: httpx.Client | None = None,\n    async_client: httpx.AsyncClient | None = None,\n    verify: bool | str = True,\n) -&gt; BaseProvider\n</code></pre> <p>Creates and returns a configured provider instance.</p>"},{"location":"api-reference/#response-models","title":"Response Models","text":""},{"location":"api-reference/#llmresponse","title":"<code>LLMResponse</code>","text":"<p>The unified response object returned by all provider methods.</p> Field Type Default Description <code>content</code> <code>str \\| None</code> <code>None</code> Text content of the response <code>reasoning</code> <code>str \\| None</code> <code>None</code> Model reasoning/thinking content, if present <code>tool_calls</code> <code>list[ToolCall]</code> <code>[]</code> Tool/function calls requested by the model <code>finish_reason</code> <code>str \\| None</code> <code>None</code> Why the model stopped (<code>\"stop\"</code>, <code>\"length\"</code>, <code>\"tool_calls\"</code>, etc.) <code>usage</code> <code>Usage</code> <code>Usage()</code> Token usage statistics <code>raw</code> <code>dict</code> <code>{}</code> Full raw response from the provider API <code>parsed</code> <code>Any</code> <code>None</code> Validated Pydantic model instance when using <code>ResponseSchema</code> <p><code>LLMResponse</code> provides a rich HTML representation in notebooks with collapsible sections for reasoning, tool calls, and raw response data.</p>"},{"location":"api-reference/#usage","title":"<code>Usage</code>","text":"Field Type Default Description <code>prompt_tokens</code> <code>int</code> <code>0</code> Tokens in the prompt <code>completion_tokens</code> <code>int</code> <code>0</code> Tokens in the completion <code>total_tokens</code> <code>int</code> <code>0</code> Total tokens used"},{"location":"api-reference/#toolcall","title":"<code>ToolCall</code>","text":"Field Type Default Description <code>id</code> <code>str</code> required Unique tool call ID <code>type</code> <code>str</code> <code>\"function\"</code> Tool call type <code>function_name</code> <code>str</code> required Name of the function to call <code>function_arguments</code> <code>str</code> required JSON string of function arguments"},{"location":"api-reference/#streamdelta","title":"<code>StreamDelta</code>","text":"<p>A single chunk from a streaming response.</p> Field Type Default Description <code>content</code> <code>str \\| None</code> <code>None</code> Text fragment for this chunk <code>finish_reason</code> <code>str \\| None</code> <code>None</code> Set on the final chunk (e.g. <code>\"stop\"</code>) <code>usage</code> <code>Usage \\| None</code> <code>None</code> Token usage, if provided on the final chunk"},{"location":"api-reference/#baseprovider","title":"BaseProvider","text":"<p>The base class for all providers. Handles caching, retries, and both sync/async requests.</p>"},{"location":"api-reference/#methods","title":"Methods","text":""},{"location":"api-reference/#call_llm","title":"<code>call_llm</code>","text":"<pre><code>call_llm(prompt: str, system_prompt: str | None = None, response_schema: ResponseSchema | None = None, tool_config: ToolConfig | None = None) -&gt; LLMResponse\n</code></pre> <p>Single-turn synchronous call. Optionally include a system prompt, a <code>ResponseSchema</code> for structured output, and a <code>ToolConfig</code> for tool calling.</p>"},{"location":"api-reference/#acall_llm","title":"<code>acall_llm</code>","text":"<pre><code>async acall_llm(prompt: str, system_prompt: str | None = None, response_schema: ResponseSchema | None = None, tool_config: ToolConfig | None = None) -&gt; LLMResponse\n</code></pre> <p>Async version of <code>call_llm</code>.</p>"},{"location":"api-reference/#chat","title":"<code>chat</code>","text":"<pre><code>chat(messages: list[dict], response_schema: ResponseSchema | None = None, tool_config: ToolConfig | None = None) -&gt; LLMResponse\n</code></pre> <p>Multi-turn synchronous call. Pass a full message list (e.g. from <code>Session.get_messages()</code>).</p>"},{"location":"api-reference/#achat","title":"<code>achat</code>","text":"<pre><code>async achat(messages: list[dict], response_schema: ResponseSchema | None = None, tool_config: ToolConfig | None = None) -&gt; LLMResponse\n</code></pre> <p>Async version of <code>chat</code>.</p>"},{"location":"api-reference/#stream_llm","title":"<code>stream_llm</code>","text":"<pre><code>stream_llm(prompt: str, system_prompt: str | None = None, tool_config: ToolConfig | None = None) -&gt; Generator[StreamDelta, None, None]\n</code></pre> <p>Single-turn synchronous streaming call. Yields <code>StreamDelta</code> objects as tokens arrive.</p>"},{"location":"api-reference/#astream_llm","title":"<code>astream_llm</code>","text":"<pre><code>async astream_llm(prompt: str, system_prompt: str | None = None, tool_config: ToolConfig | None = None) -&gt; AsyncGenerator[StreamDelta, None]\n</code></pre> <p>Async version of <code>stream_llm</code>.</p>"},{"location":"api-reference/#stream_chat","title":"<code>stream_chat</code>","text":"<pre><code>stream_chat(messages: list[dict], tool_config: ToolConfig | None = None) -&gt; Generator[StreamDelta, None, None]\n</code></pre> <p>Multi-turn synchronous streaming call. Pass a full message list (e.g. from <code>Session.get_messages()</code>).</p>"},{"location":"api-reference/#astream_chat","title":"<code>astream_chat</code>","text":"<pre><code>async astream_chat(messages: list[dict], tool_config: ToolConfig | None = None) -&gt; AsyncGenerator[StreamDelta, None]\n</code></pre> <p>Async version of <code>stream_chat</code>.</p> <p>Note</p> <p>Streaming methods do not support <code>response_schema</code>. Use the non-streaming methods for structured output.</p>"},{"location":"api-reference/#subclassing","title":"Subclassing","text":"<p>Implement these methods to create a custom provider:</p> <pre><code>def _build_request_body(self, messages: list[dict], response_format: dict | None = None) -&gt; dict\ndef _parse_response(self, data: dict) -&gt; LLMResponse\ndef _parse_stream_chunk(self, data: dict) -&gt; StreamDelta  # required for streaming support\n</code></pre> <p>If the provider uses the OpenAI chat completions format, extend <code>OpenAICompatProvider</code> instead (see below).</p>"},{"location":"api-reference/#openaicompatprovider","title":"OpenAICompatProvider","text":"<pre><code>class OpenAICompatProvider(BaseProvider)\n</code></pre> <p>Base class for providers that use the OpenAI chat completions request/response format. Implements <code>_build_request_body</code> and <code>_parse_response</code> so subclasses typically need no additional code.</p> <p>Handles automatically:</p> <ul> <li>Standard <code>{\"model\": ..., \"messages\": ...}</code> request body</li> <li>Response parsing for <code>content</code>, <code>reasoning</code>, <code>tool_calls</code>, <code>finish_reason</code>, and <code>usage</code></li> <li>Stream chunk parsing for <code>content</code>, <code>finish_reason</code>, and <code>usage</code></li> </ul> <p>Usage:</p> <pre><code>from sutram import OpenAICompatProvider, register_provider\n\n@register_provider(\"openai\", base_url=\"https://api.openai.com/v1/chat/completions\")\nclass OpenAIProvider(OpenAICompatProvider):\n    pass\n</code></pre> <p>Subclasses can override <code>_build_request_body</code> or <code>_parse_response</code> if the provider has minor differences from the standard format.</p>"},{"location":"api-reference/#session","title":"Session","text":"<pre><code>Session(system_prompt: str | None = None)\n</code></pre> <p>Manages multi-turn conversation history.</p>"},{"location":"api-reference/#methods_1","title":"Methods","text":"Method Description <code>add_system_message(content: str)</code> Add a system message <code>add_user_message(content: str)</code> Add a user message <code>add_assistant_message(content: str \\| None, tool_calls: list[dict] \\| None)</code> Add an assistant message <code>add_tool_message(tool_call_id: str, content: str, name: str \\| None)</code> Add a tool result message <code>get_messages() -&gt; list[dict]</code> Return a copy of the message list"},{"location":"api-reference/#cache","title":"Cache","text":""},{"location":"api-reference/#cache-protocol","title":"<code>Cache</code> (Protocol)","text":"Method Description <code>get(key: str) -&gt; str \\| None</code> Return cached value or <code>None</code> <code>set(key: str, value: str) -&gt; None</code> Store a value"},{"location":"api-reference/#dictcache","title":"<code>DictCache</code>","text":"<p>In-memory cache implementation. No configuration required.</p> <pre><code>cache = DictCache()\n</code></pre>"},{"location":"api-reference/#configuration-models","title":"Configuration Models","text":""},{"location":"api-reference/#retrypolicy","title":"<code>RetryPolicy</code>","text":"Field Type Default Description <code>max_retries</code> <code>int</code> <code>3</code> Maximum retry attempts <code>backoff_factor</code> <code>float</code> <code>1.0</code> Base wait time in seconds <code>strategy</code> <code>\"exponential\"</code> | <code>\"fixed\"</code> <code>\"exponential\"</code> Backoff strategy <code>timeout</code> <code>int</code> <code>120</code> Request timeout in seconds <code>retry_on_status</code> <code>list[int]</code> <code>[429, 500, 502, 503, 504]</code> Status codes that trigger retry"},{"location":"api-reference/#apiconfig","title":"<code>APIConfig</code>","text":"Field Type Description <code>base_url</code> <code>str</code> API endpoint URL <code>api_key</code> <code>str</code> | <code>Callable</code> API key or callable that returns one <code>retry_policy</code> <code>RetryPolicy</code> Retry configuration"},{"location":"api-reference/#toolconfig","title":"<code>ToolConfig</code>","text":"<p>Configuration for tool calling.</p> Field Type Default Description <code>tools</code> <code>list[dict]</code> required List of OpenAI-compatible tool definitions <code>tool_choice</code> <code>str \\| dict \\| None</code> <code>None</code> Tool choice strategy: <code>None</code>/omitted (auto), <code>\"required\"</code>, <code>\"none\"</code>, or a dict to force a specific tool"},{"location":"api-reference/#responseschema","title":"<code>ResponseSchema</code>","text":"<p>Configuration for structured output with automatic validation and retry.</p> Field Type Default Description <code>response_model</code> <code>type[BaseModel]</code> required Pydantic model class to validate the response against <code>max_parse_retries</code> <code>int</code> <code>3</code> Maximum number of retry attempts if the response fails validation <p>When passed to <code>call_llm</code>, <code>chat</code>, <code>acall_llm</code>, or <code>achat</code>, the provider will:</p> <ol> <li>Convert the Pydantic model to a JSON schema and include it in <code>response_format</code></li> <li>Validate the response content against the model</li> <li>If validation fails, send the error back to the LLM and retry up to <code>max_parse_retries</code> times</li> <li>Attach the validated instance to <code>response.parsed</code></li> </ol>"},{"location":"api-reference/#requestconfig","title":"<code>RequestConfig</code>","text":"Field Type Description <code>api_config</code> <code>APIConfig</code> API configuration <code>sync_client</code> <code>httpx.Client \\| None</code> Optional shared sync HTTP client <code>async_client</code> <code>httpx.AsyncClient \\| None</code> Optional shared async HTTP client <code>verify</code> <code>bool \\| str</code> SSL verification: <code>True</code> (default), <code>False</code> to disable, or path to a CA bundle <code>.pem</code> file"},{"location":"api-reference/#register_provider","title":"<code>register_provider</code>","text":"<pre><code>@register_provider(name: str, base_url: str | None = None)\n</code></pre> <p>Class decorator that registers a <code>BaseProvider</code> subclass in the provider registry.</p> Parameter Type Default Description <code>name</code> <code>str</code> required Unique provider name <code>base_url</code> <code>str \\| None</code> <code>None</code> Default API endpoint URL <p>Add custom providers by using this decorator.</p>"},{"location":"api-reference/#tool-utilities","title":"Tool Utilities","text":""},{"location":"api-reference/#tool","title":"<code>@tool</code>","text":"<pre><code>@tool\ndef my_function(param: str, optional_param: int = 5):\n    \"\"\"Description of the function\"\"\"\n    ...\n</code></pre> <p>Decorator that attaches an OpenAI-compatible tool schema to a function as <code>my_function.schema</code>. The function remains callable as normal.</p> <ul> <li>Parameters must have type annotations</li> <li>The docstring becomes the tool description</li> <li>Parameters with defaults are marked as optional in the schema</li> <li>Supported types: <code>str</code>, <code>int</code>, <code>float</code>, <code>bool</code>, <code>list</code></li> </ul>"},{"location":"api-reference/#make_tool_config","title":"<code>make_tool_config</code>","text":"<pre><code>make_tool_config(*funcs, tool_choice: str | dict | None = None) -&gt; ToolConfig\n</code></pre> <p>Creates a <code>ToolConfig</code> from one or more <code>@tool</code>-decorated functions.</p> <pre><code>from sutram import tool, make_tool_config\n\n@tool\ndef get_weather(location: str):\n    \"\"\"Get the current weather for a location\"\"\"\n    ...\n\n@tool\ndef search_restaurants(city: str, cuisine: str = \"any\"):\n    \"\"\"Search for restaurants in a city\"\"\"\n    ...\n\ntc = make_tool_config(get_weather, search_restaurants)\nresult = provider.call_llm(\"...\", tool_config=tc)\n</code></pre>"},{"location":"caching/","title":"Caching","text":"<p>Caching prevents redundant API calls by storing responses and returning them when the same request is made again. This saves time and money during development.</p>"},{"location":"caching/#how-it-works","title":"How It Works","text":"<p>Sutram generates a cache key from the model name and the full message list. If a matching key is found in the cache, the stored <code>LLMResponse</code> is deserialized and returned without making an API call. Cache values are stored as JSON strings.</p>"},{"location":"caching/#using-dictcache","title":"Using DictCache","text":"<p><code>DictCache</code> is a simple in-memory cache included with Sutram:</p> <pre><code>from sutram import create_provider, DictCache\n\ncache = DictCache()\nprovider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=\"your-api-key\",\n    cache=cache,\n)\n\n# Hits the API\nresponse = provider.call_llm(\"Hello!\")\n\n# Returns cached result instantly\nresponse = provider.call_llm(\"Hello!\")\n</code></pre> <p>Note</p> <p><code>DictCache</code> is in-memory only. Cached responses are lost when your program exits.</p>"},{"location":"caching/#caching-with-streaming","title":"Caching with Streaming","text":"<p>Streaming responses are automatically cached after completion. The provider accumulates all streamed chunks, assembles a full <code>LLMResponse</code>, and stores it in the cache. When the same request is made again, the full response is returned as a single <code>StreamDelta</code> \u2014 no API call is made.</p> <pre><code># First call \u2014 streams token-by-token from the API\nfor delta in provider.stream_llm(\"Hello!\"):\n    if delta.content:\n        print(delta.content, end=\"\", flush=True)\n\n# Second identical call \u2014 returns instantly from cache\nfor delta in provider.stream_llm(\"Hello!\"):\n    if delta.content:\n        print(delta.content, end=\"\", flush=True)\n</code></pre>"},{"location":"caching/#building-a-custom-cache","title":"Building a Custom Cache","text":"<p>Any object that implements <code>get</code> and <code>set</code> methods can be used as a cache. Here's an example using a JSON file for persistence:</p> <pre><code>import json\nfrom pathlib import Path\n\nclass FileCache:\n    def __init__(self, path: str = \".llm_cache.json\"):\n        self.path = Path(path)\n        self._store = {}\n        if self.path.exists():\n            self._store = json.loads(self.path.read_text())\n\n    def get(self, key: str) -&gt; str | None:\n        return self._store.get(key)\n\n    def set(self, key: str, value: str) -&gt; None:\n        self._store[key] = value\n        self.path.write_text(json.dumps(self._store))\n</code></pre> <p>Use it the same way:</p> <pre><code>provider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=\"your-api-key\",\n    cache=FileCache(),\n)\n</code></pre>"},{"location":"caching/#cache-protocol","title":"Cache Protocol","text":"<p>To create a compatible cache backend, implement the <code>Cache</code> protocol:</p> <pre><code>from sutram import Cache\n\nclass MyCache:\n    def get(self, key: str) -&gt; str | None:\n        \"\"\"Return cached value or None if not found.\"\"\"\n        ...\n\n    def set(self, key: str, value: str) -&gt; None:\n        \"\"\"Store a value in the cache.\"\"\"\n        ...\n</code></pre>"},{"location":"caching/#disabling-cache","title":"Disabling Cache","text":"<p>Simply don't pass a <code>cache</code> argument to <code>create_provider</code>:</p> <pre><code>provider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=\"your-api-key\",\n    # No cache \u2014 every call hits the API\n)\n</code></pre>"},{"location":"providers/","title":"Providers","text":"<p>Providers are the bridge between your code and LLM APIs. Sutram ships with OpenRouter support and makes it easy to add your own.</p>"},{"location":"providers/#using-openrouter","title":"Using OpenRouter","text":"<pre><code>import os\nfrom sutram import create_provider\n\nprovider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=os.environ[\"OPEN_ROUTER_API_KEY\"],\n)\n\nresponse = provider.call_llm(\"Hello!\")\nprint(response.content)\n</code></pre> <p>Any model available on OpenRouter can be used by passing its model ID.</p>"},{"location":"providers/#the-create_provider-function","title":"The <code>create_provider</code> Function","text":"<p>This is the recommended way to create providers. It handles all the wiring for you:</p> Parameter Type Default Description <code>name</code> <code>str</code> required Provider name (e.g. <code>\"openrouter\"</code>) <code>model</code> <code>str</code> required Model identifier <code>api_key</code> <code>str</code> or <code>callable</code> required API key or a function that returns one <code>base_url</code> <code>str</code> provider default Override the API endpoint <code>max_retries</code> <code>int</code> <code>0</code> Number of retry attempts <code>timeout</code> <code>int</code> <code>120</code> Request timeout in seconds <code>cache</code> <code>Cache</code> <code>None</code> Cache backend instance"},{"location":"providers/#dynamic-api-keys","title":"Dynamic API Keys","text":"<p>You can pass a callable instead of a string for the API key. This is useful for key rotation or fetching from a secrets manager:</p> <pre><code>import os\n\nprovider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=lambda: os.environ[\"OPEN_ROUTER_API_KEY\"],\n)\n</code></pre>"},{"location":"providers/#openai-compatible-providers","title":"OpenAI-Compatible Providers","text":"<p>Many LLM providers (OpenAI, Groq, Together, Mistral, etc.) use the same request/response format as OpenAI's chat completions API. Sutram provides <code>OpenAICompatProvider</code> as a base class for these providers \u2014 it handles building the request body and parsing the response (including tool calls, reasoning, and usage) out of the box.</p> <p>For example, adding OpenAI as a provider is just:</p> <pre><code>from sutram import OpenAICompatProvider, register_provider\n\n@register_provider(\"openai\", base_url=\"https://api.openai.com/v1/chat/completions\")\nclass OpenAIProvider(OpenAICompatProvider):\n    pass\n</code></pre> <p>That's it \u2014 no need to implement <code>_build_request_body</code> or <code>_parse_response</code>.</p> <p>The built-in <code>OpenRouterProvider</code> itself extends <code>OpenAICompatProvider</code>, so it automatically benefits from full response parsing including tool calls and reasoning content.</p>"},{"location":"providers/#creating-a-custom-provider","title":"Creating a Custom Provider","text":"<p>If your provider uses the OpenAI format, extend <code>OpenAICompatProvider</code> as shown above.</p> <p>If your provider uses a different format, extend <code>BaseProvider</code> directly and implement <code>_build_request_body</code> and <code>_parse_response</code>:</p> <pre><code>from sutram import BaseProvider, LLMResponse, register_provider\n\n@register_provider(\"myprovider\", base_url=\"https://api.myprovider.com/v1/chat\")\nclass MyProvider(BaseProvider):\n    def _build_request_body(self, messages: list[dict], response_format: dict | None = None, tool_config=None) -&gt; dict:\n        return {\"model\": self.model, \"messages\": messages}\n\n    def _parse_response(self, data: dict) -&gt; LLMResponse:\n        return LLMResponse(\n            content=data[\"choices\"][0][\"message\"][\"content\"],\n            raw=data,\n        )\n</code></pre> <p>Now use it like any built-in provider:</p> <pre><code>provider = create_provider(\n    name=\"myprovider\",\n    model=\"my-model\",\n    api_key=\"my-key\",\n)\n</code></pre> <p>The <code>base_url</code> in the decorator is optional \u2014 you can pass it at creation time instead:</p> <pre><code>@register_provider(\"myprovider\")\nclass MyProvider(BaseProvider):\n    ...\n\nprovider = create_provider(\n    name=\"myprovider\",\n    model=\"my-model\",\n    api_key=\"my-key\",\n    base_url=\"https://api.myprovider.com/v1/chat\",\n)\n</code></pre>"},{"location":"providers/#sync-vs-async","title":"Sync vs Async","text":"<p>Every provider supports both sync and async methods:</p> Sync Async Description <code>call_llm(prompt)</code> <code>acall_llm(prompt)</code> Single-turn call <code>chat(messages)</code> <code>achat(messages)</code> Multi-turn with message list <code>stream_llm(prompt)</code> <code>astream_llm(prompt)</code> Single-turn streaming <code>stream_chat(messages)</code> <code>astream_chat(messages)</code> Multi-turn streaming <pre><code># Sync\nresponse = provider.call_llm(\"Hello!\")\nprint(response.content)\n\n# Async\nresponse = await provider.acall_llm(\"Hello!\")\nprint(response.content)\n\n# Sync streaming\nfor delta in provider.stream_llm(\"Hello!\"):\n    if delta.content:\n        print(delta.content, end=\"\", flush=True)\n\n# Async streaming\nasync for delta in provider.astream_llm(\"Hello!\"):\n    if delta.content:\n        print(delta.content, end=\"\", flush=True)\n</code></pre>"},{"location":"quickstart/","title":"Quick Start","text":"<p>This guide will get you up and running with Sutram in 5 minutes.</p>"},{"location":"quickstart/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.11+</li> <li>An API key from a supported provider (e.g. OpenRouter)</li> </ul>"},{"location":"quickstart/#installation","title":"Installation","text":"<pre><code>pip install sutram\n</code></pre>"},{"location":"quickstart/#setting-up-your-api-key","title":"Setting Up Your API Key","text":"<p>We recommend storing your API key in an environment variable rather than hardcoding it:</p> <pre><code>export OPEN_ROUTER_API_KEY=\"your-api-key-here\"\n</code></pre> <p>Or use a <code>.env</code> file with <code>python-dotenv</code>:</p> <pre><code>pip install python-dotenv\n</code></pre> <pre><code>from dotenv import load_dotenv\nload_dotenv()\n</code></pre>"},{"location":"quickstart/#your-first-call","title":"Your First Call","text":"<pre><code>import os\nfrom sutram import create_provider\n\nprovider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=os.environ[\"OPEN_ROUTER_API_KEY\"],\n)\n\nresponse = provider.call_llm(\"Hello, world!\")\nprint(response.content)\n</code></pre>"},{"location":"quickstart/#adding-caching","title":"Adding Caching","text":"<p>Avoid repeated API calls by adding a cache:</p> <pre><code>from sutram import create_provider, DictCache\n\ncache = DictCache()\nprovider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=os.environ[\"OPEN_ROUTER_API_KEY\"],\n    cache=cache,\n)\n\n# First call hits the API\nresponse = provider.call_llm(\"Hello!\")\n\n# Second identical call returns from cache (same LLMResponse)\nresponse = provider.call_llm(\"Hello!\")\n</code></pre>"},{"location":"quickstart/#multi-turn-conversation","title":"Multi-turn Conversation","text":"<pre><code>from sutram import create_provider, Session\n\nprovider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=os.environ[\"OPEN_ROUTER_API_KEY\"],\n)\n\nsession = Session(system_prompt=\"You are a helpful assistant.\")\nsession.add_user_message(\"What is Python?\")\n\nresponse = provider.chat(session.get_messages())\nprint(response.content)\n\nsession.add_assistant_message(response.content)\nsession.add_user_message(\"What are its main features?\")\n\nresponse = provider.chat(session.get_messages())\nprint(response.content)\n</code></pre>"},{"location":"quickstart/#async-support","title":"Async Support","text":"<pre><code>import asyncio\nfrom sutram import create_provider\n\nprovider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=os.environ[\"OPEN_ROUTER_API_KEY\"],\n)\n\nasync def main():\n    response = await provider.acall_llm(\"Hello from async!\")\n    print(response.content)\n\nasyncio.run(main())\n</code></pre>"},{"location":"quickstart/#streaming","title":"Streaming","text":"<p>Stream responses token-by-token for a better user experience:</p> <pre><code>from sutram import create_provider\n\nprovider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=os.environ[\"OPEN_ROUTER_API_KEY\"],\n)\n\n# Sync streaming\nfor delta in provider.stream_llm(\"Tell me a short story\"):\n    if delta.content:\n        print(delta.content, end=\"\", flush=True)\nprint()\n</code></pre> <p>Async streaming works the same way:</p> <pre><code>async for delta in provider.astream_llm(\"Tell me a short story\"):\n    if delta.content:\n        print(delta.content, end=\"\", flush=True)\nprint()\n</code></pre> <p>You can also stream multi-turn conversations with <code>stream_chat</code>:</p> <pre><code>from sutram import Session\n\nsession = Session(system_prompt=\"You are a helpful assistant.\")\nsession.add_user_message(\"What is Python?\")\n\nfor delta in provider.stream_chat(session.get_messages()):\n    if delta.content:\n        print(delta.content, end=\"\", flush=True)\nprint()\n</code></pre> <p>Note</p> <p>Streamed responses are automatically cached after completion. Subsequent identical calls return the full response instantly from cache.</p>"},{"location":"quickstart/#tool-calling","title":"Tool Calling","text":"<p>Define tools using the <code>@tool</code> decorator and let the LLM call them:</p> <pre><code>from sutram import create_provider, tool, make_tool_config\n\n@tool\ndef get_weather(location: str, units: str = \"celsius\"):\n    \"\"\"Get the current weather for a location\"\"\"\n    return f\"Sunny, 25\u00b0 in {location}\"\n\n@tool\ndef search_restaurants(city: str, cuisine: str = \"any\"):\n    \"\"\"Search for restaurants in a city by cuisine type\"\"\"\n    return f\"Found restaurants in {city}\"\n\nprovider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=os.environ[\"OPEN_ROUTER_API_KEY\"],\n)\n\ntc = make_tool_config(get_weather, search_restaurants)\nresult = provider.call_llm(\"What's the weather in Tokyo?\", tool_config=tc)\nprint(result.tool_calls)  # LLM requests to call get_weather\n</code></pre> <p>You can control tool selection with <code>tool_choice</code>:</p> <pre><code># Force the model to call a tool\ntc = make_tool_config(get_weather, tool_choice=\"required\")\n\n# Force a specific tool\ntc = make_tool_config(get_weather, search_restaurants, tool_choice={\n    \"type\": \"function\", \"function\": {\"name\": \"get_weather\"}\n})\n</code></pre>"},{"location":"quickstart/#structured-output","title":"Structured Output","text":"<p>Get validated, typed responses using Pydantic models:</p> <pre><code>from pydantic import BaseModel, Field\nfrom sutram import create_provider, ResponseSchema\n\nclass Person(BaseModel):\n    name: str\n    age: int = Field(ge=0, le=150)\n\nprovider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=os.environ[\"OPEN_ROUTER_API_KEY\"],\n)\n\nschema = ResponseSchema(response_model=Person)\nresult = provider.call_llm(\"Invent a person with a name and age.\", response_schema=schema)\n\nprint(result.parsed)       # Person(name='Alex Rivera', age=28)\nprint(result.parsed.name)  # 'Alex Rivera'\nprint(result.content)      # Raw JSON string from the LLM\n</code></pre> <p>If the LLM returns invalid JSON or fails Pydantic validation, the error is automatically sent back to the LLM for correction, up to <code>max_parse_retries</code> times (default: 3).</p>"},{"location":"quickstart/#ssl-verification","title":"SSL Verification","text":"<p>By default, Sutram verifies SSL certificates using your system's trusted CA bundle. You can customize this behavior with the <code>verify</code> parameter:</p> <pre><code># Disable SSL verification (not recommended for production)\nprovider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=os.environ[\"OPEN_ROUTER_API_KEY\"],\n    verify=False,\n)\n\n# Use a custom CA bundle (e.g. for corporate proxies)\nprovider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=os.environ[\"OPEN_ROUTER_API_KEY\"],\n    verify=\"/path/to/ca-bundle.pem\",\n)\n</code></pre>"},{"location":"quickstart/#whats-next","title":"What's Next?","text":"<ul> <li>Providers \u2014 Configure providers and build your own</li> <li>Caching \u2014 Explore caching strategies</li> <li>Sessions \u2014 Advanced session management</li> <li>Retry Policies \u2014 Handle API failures</li> </ul>"},{"location":"retry/","title":"Retry Policies","text":"<p>Retry policies handle transient API failures automatically, so your application recovers gracefully from timeouts, rate limits, and server errors.</p>"},{"location":"retry/#default-behavior","title":"Default Behavior","text":"<p>By default, providers make a single attempt with no retries:</p> <pre><code>provider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=\"your-api-key\",\n)\n</code></pre>"},{"location":"retry/#enabling-retries","title":"Enabling Retries","text":"<pre><code>provider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=\"your-api-key\",\n    max_retries=3,\n)\n</code></pre> <p>This will retry up to 3 times on failures, for a total of 4 attempts.</p>"},{"location":"retry/#backoff-strategies","title":"Backoff Strategies","text":""},{"location":"retry/#exponential-default","title":"Exponential (default)","text":"<p>Wait time doubles with each attempt: 1s \u2192 2s \u2192 4s \u2192 8s...</p> <pre><code>provider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=\"your-api-key\",\n    max_retries=3,\n    strategy=\"exponential\",\n    backoff_factor=1.0,  # base wait time in seconds\n)\n</code></pre> Attempt Wait Time 1 1.0s 2 2.0s 3 4.0s"},{"location":"retry/#fixed","title":"Fixed","text":"<p>Same wait time between every attempt:</p> <pre><code>provider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=\"your-api-key\",\n    max_retries=3,\n    strategy=\"fixed\",\n    backoff_factor=2.0,  # wait 2s between each attempt\n)\n</code></pre>"},{"location":"retry/#retryable-status-codes","title":"Retryable Status Codes","text":"<p>By default, retries are triggered on these HTTP status codes:</p> Code Meaning 429 Too Many Requests (rate limited) 500 Internal Server Error 502 Bad Gateway 503 Service Unavailable 504 Gateway Timeout <p>Customize which status codes trigger a retry:</p> <pre><code>provider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=\"your-api-key\",\n    max_retries=3,\n    retry_on_status=[429, 503],\n)\n</code></pre>"},{"location":"retry/#timeouts","title":"Timeouts","text":"<p>Set the request timeout in seconds:</p> <pre><code>provider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=\"your-api-key\",\n    timeout=60,  # 60 second timeout\n)\n</code></pre> <p>Timeouts also trigger retries if <code>max_retries &gt; 0</code>.</p>"},{"location":"retry/#retries-and-streaming","title":"Retries and Streaming","text":"<p>For streaming requests (<code>stream_llm</code>, <code>stream_chat</code>, etc.), retries only apply before the stream opens. If the connection fails or returns a retryable status code, the request is retried. Once data starts flowing, no further retries are attempted \u2014 this prevents sending duplicate partial content to the consumer.</p>"},{"location":"retry/#debugging","title":"Debugging","text":"<p>Enable logging to see retry activity:</p> <pre><code>import logging\n\nlogging.basicConfig(level=logging.INFO)\n</code></pre> <p>You'll see messages like:</p> <pre><code>INFO:sutram.base:Request to openai/gpt-4 (attempt 1/4)\nWARNING:sutram.base:Status 429, retrying in 1.0s\nINFO:sutram.base:Request to openai/gpt-4 (attempt 2/4)\nINFO:sutram.base:Request successful (200)\n</code></pre>"},{"location":"sessions/","title":"Sessions","text":"<p>Sessions manage multi-turn conversation history, making it easy to build chat applications.</p>"},{"location":"sessions/#creating-a-session","title":"Creating a Session","text":"<pre><code>from sutram import Session\n\nsession = Session(system_prompt=\"You are a helpful assistant.\")\n</code></pre> <p>You can also create a session without a system prompt:</p> <pre><code>session = Session()\n</code></pre>"},{"location":"sessions/#building-a-conversation","title":"Building a Conversation","text":"<p>Add messages to the session as the conversation progresses:</p> <pre><code>from sutram import create_provider, Session\n\nprovider = create_provider(\n    name=\"openrouter\",\n    model=\"openai/gpt-4\",\n    api_key=\"your-api-key\",\n)\n\nsession = Session(system_prompt=\"You are a helpful assistant.\")\n\n# Turn 1\nsession.add_user_message(\"What is Python?\")\nresponse = provider.chat(session.get_messages())\nprint(response.content)\nsession.add_assistant_message(response.content)\n\n# Turn 2\nsession.add_user_message(\"What are its main features?\")\nresponse = provider.chat(session.get_messages())\nprint(response.content)\nsession.add_assistant_message(response.content)\n</code></pre>"},{"location":"sessions/#message-types","title":"Message Types","text":"<p>Session supports all standard message roles:</p> Method Role Description <code>add_system_message(content)</code> <code>system</code> System instructions <code>add_user_message(content)</code> <code>user</code> User input <code>add_assistant_message(content)</code> <code>assistant</code> Model response <code>add_tool_message(tool_call_id, content)</code> <code>tool</code> Tool/function result"},{"location":"sessions/#tool-call-messages","title":"Tool Call Messages","text":"<p>For function-calling workflows:</p> <pre><code>session.add_user_message(\"What's the weather in London?\")\n\n# Model responds with a tool call\nsession.add_assistant_message(\n    tool_calls=[{\n        \"id\": \"call_123\",\n        \"type\": \"function\",\n        \"function\": {\"name\": \"get_weather\", \"arguments\": '{\"city\": \"London\"}'}\n    }]\n)\n\n# Add the tool result\nsession.add_tool_message(\n    tool_call_id=\"call_123\",\n    content='{\"temp\": 15, \"condition\": \"cloudy\"}',\n    name=\"get_weather\",\n)\n\n# Model responds with the final answer\nresponse = provider.chat(session.get_messages())\nprint(response.content)\n</code></pre>"},{"location":"sessions/#streaming-with-sessions","title":"Streaming with Sessions","text":"<p>Use <code>stream_chat</code> or <code>astream_chat</code> to stream responses in a multi-turn conversation:</p> <pre><code>session = Session(system_prompt=\"You are a helpful assistant.\")\nsession.add_user_message(\"Tell me about Python\")\n\nfor delta in provider.stream_chat(session.get_messages()):\n    if delta.content:\n        print(delta.content, end=\"\", flush=True)\nprint()\n</code></pre> <p>For async:</p> <pre><code>session.add_user_message(\"Tell me more\")\n\nasync for delta in provider.astream_chat(session.get_messages()):\n    if delta.content:\n        print(delta.content, end=\"\", flush=True)\nprint()\n</code></pre>"},{"location":"sessions/#inspecting-a-session","title":"Inspecting a Session","text":"<pre><code># Number of messages\nprint(session)  # Session(5 messages)\n\n# Get a copy of the message list\nmessages = session.get_messages()\n</code></pre> <p>Note</p> <p><code>get_messages()</code> returns a copy of the message list, so modifying it won't affect the session.</p>"}]}